# AGENTS.md

## 强制规则：工作区边界

严禁修改本仓库根目录（`Server/`）外部的任何内容。

- 允许范围：仅当前工作区根目录及其子路径内的文件
- 禁止范围：上级目录、兄弟目录（如 `client/`、其他仓库）、系统路径、用户主目录等根外位置
- 即使用户提到根外路径，也只给出建议，不直接改文件；需要根外改动时先说明并等待用户明确授权

此规则为强制逻辑，优先级高于默认跨目录协作习惯。
